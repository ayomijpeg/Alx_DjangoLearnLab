# (You should still have the 'book' variable from the last step)
# Update the title
>>> book.title = "Nineteen Eighty-Four"

# Save the change
>>> book.save()

# Verify the change (optional)
>>> print(book)
<Book: Nineteen Eighty-Four>
